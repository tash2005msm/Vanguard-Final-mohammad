name: Vanguard Cloud Build
on: 
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Start Build Process
        run: |
          echo "Building for Mohammad..."
          # هذا أمر وهمي سريع للتأكد من أن السيرفر يعمل
          echo "Success"
